syntax="proto3";

option go_package = "bakerypb";

message MacaroonId {
	bytes nonce = 1;
	bytes storageId = 2;
	repeated Op ops = 3;
}

message Op {
	string entity = 1;
	repeated string actions = 2;
}

message ThirdPartyCondition {
	bytes body = 1;
	repeated string needDeclared = 2;
}
